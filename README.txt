If you want to install and run it, you'll need next libraries:
	1)cv2 (pip install opencv-python)
	2)numpy (pip install numpy)
	3)numba (pip install numba)
	4)random
	5)sys
To run, just run art.py and then write the path to the image such as 'test1.jpg'